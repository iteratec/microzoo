name: postgres
description: PostgreSQL database
constants:
  userName: postgres
  password: root_pw
  port: 5432
  db: postgres
docker:
  image: postgres:13-alpine
  environment:
    POSTGRES_PASSWORD: root_pw
